{
  "rules": {
    ".read": false,
    ".write": false,
    "jobs": {
      ".indexOn": ["ownerId", "postedAt", "employmentType", "location"],
      "$jobId": {
        ".read": true,
        ".write": "auth != null && (( !data.exists() && newData.child('ownerId').val() === auth.uid ) || ( data.exists() && data.child('ownerId').val() === auth.uid ))"
      }
    },
    "applications": {
      "$jobId": {
        "$appId": {
          ".read": "auth != null && (data.child('userId').val() === auth.uid || data.child('jobOwnerId').val() === auth.uid)",
          ".write": "auth != null && (( !data.exists() && newData.child('userId').val() === auth.uid ) || ( data.exists() && data.child('jobOwnerId').val() === auth.uid ))"
        }
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
